import requests

# The path to the image you want to test
image_path = "dataset/TB Chest X-rays/some_image.png" 

url = "http://localhost:5000/predict"

with open(image_path, 'rb') as f:
    response = requests.post(url, files={'file': f})

print(response.json())